const { getCommitlintConfig } = require('@applint/spec');

module.exports = getCommitlintConfig('common');

const { getCommitlintConfig } = require('@applint/spec');

module.exports = getCommitlintConfig('common');
